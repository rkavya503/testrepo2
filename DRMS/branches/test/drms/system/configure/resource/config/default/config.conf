# Directory on Apache Server where a new directory
# will be created (name=hostname). Static content
# such as flex and images will be placed here.
staticRemoteDir=/var/www/virtual
staticRemoteServers=
