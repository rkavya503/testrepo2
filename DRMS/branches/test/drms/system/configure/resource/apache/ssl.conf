#
# This is the Apache server configuration file providing SSL support.
# It contains the configuration directives to instruct the server how to
# serve pages over an https connection. For detailing information about these 
# directives see <URL:http://httpd.apache.org/docs/2.0/mod/mod_ssl.html>
# 
# Do NOT simply read the instructions in here without understanding
# what they do.  They're here only as hints or reminders.  If you are unsure
# consult the online docs. You have been warned.  
#

#
# Pseudo Random Number Generator (PRNG):
# Configure one or more sources to seed the PRNG of the SSL library.
# The seed data should be of good random quality.
# WARNING! On some platforms /dev/random blocks if not enough entropy
# is available. This means you then cannot use the /dev/random device
# because it would lead to very long connection times (as long as
# it requires to make more entropy available). But usually those
# platforms additionally provide a /dev/urandom device which doesn't
# block. So, if available, use this one instead. Read the mod_ssl User
# Manual for more details.
#
# Note: This must come before the <IfDefine SSL> container to support
#       starting without SSL on platforms with no /dev/random equivalent
#       but a statically compiled-in mod_ssl.
#
SSLRandomSeed startup builtin
SSLRandomSeed connect builtin
#SSLRandomSeed startup file:/dev/random  512
#SSLRandomSeed startup file:/dev/urandom 512
#SSLRandomSeed connect file:/dev/random  512
#SSLRandomSeed connect file:/dev/urandom 512

#
# When we also provide SSL we have to listen to the 
# standard HTTP port (see above) and to the HTTPS port
#
# Note: Configurations that use IPv6 but not IPv4-mapped addresses need two
#       Listen directives: "Listen [::]:443" and "Listen 0.0.0.0:443"
#
Listen 443

##
##  SSL Global Context
##
##  All SSL configuration in this context applies both to
##  the main server and all SSL-enabled virtual hosts.
##

#
#   Some MIME-types for downloading Certificates and CRLs
#
AddType application/x-x509-ca-cert .crt
AddType application/x-pkcs7-crl    .crl

#   Pass Phrase Dialog:
#   Configure the pass phrase gathering process.
#   The filtering dialog program (`builtin' is a internal
#   terminal dialog) has to provide the pass phrase on stdout.
SSLPassPhraseDialog  builtin

#   Inter-Process Session Cache:
#   Configure the SSL Session Cache: First the mechanism 
#   to use and second the expiring timeout (in seconds).
#SSLSessionCache        none
#SSLSessionCache        shmht:@LOGROOT@/ssl_scache(512000)
#SSLSessionCache        shmcb:@LOGROOT@/ssl_scache(512000)
SSLSessionCache         dbm:@LOGROOT@/ssl_scache
SSLSessionCacheTimeout  300

#   Semaphore:
#   Configure the path to the mutual exclusion semaphore the
#   SSL engine uses internally for inter-process synchronization. 
SSLMutex  file:@LOGROOT@/ssl_mutex

##
## SSL Virtual Host Context
##

##
## Legacy site for backward compatibility
##
<VirtualHost @SERVER.IP.1@:443>

#   General setup for the legacy server
DocumentRoot @DOCROOT@
ServerName @SERVER.NAME.1@:443
ServerAlias localhost:443
ServerAdmin openadr@akuacom.com
ErrorLog @LOGROOT@/error.log
TransferLog @LOGROOT@/access.log

RedirectMatch ^/$ https://@SERVER.NAME.2@/pss2.website
Redirect /pss2.website https://@SERVER.NAME.2@/pss2.website

JkMountFile conf/legacy-uriworkermap.properties

SSLEngine on
SSLCipherSuite ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP:+eNULL

SSLCertificateFile @CONFROOT@/pss2-server.crt
SSLCertificateKeyFile @CONFROOT@/pss2-server.key
#SSLCertificateChainFile @CONFROOT@/gd_intermediate_bundle.crt

BrowserMatch ".*MSIE.*" \
         nokeepalive ssl-unclean-shutdown \
         downgrade-1.0 force-response-1.0

CustomLog @LOGROOT@/ssl_request.log \
          "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"

</VirtualHost>

##
## New site with good certificate
##
<VirtualHost @SERVER.IP.2@:443>

#   General setup for the virtual host
DocumentRoot "@DOCROOT@"
ServerName @SERVER.NAME.2@:443
ServerAdmin openadr@akuacom.com
ErrorLog @LOGROOT@/error_log
TransferLog @LOGROOT@/access_log

RedirectMatch ^/$ https://@SERVER.NAME.2@/pss2.website

JkMountFile conf/uriworkermap.properties

SSLEngine on
SSLCipherSuite ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP:+eNULL
SSLCertificateFile @CONFROOT@/@SERVER.NAME.2@.crt
SSLCertificateKeyFile @CONFROOT@/@SERVER.NAME.2@.key
SSLCertificateChainFile @CONFROOT@/gd_intermediate_bundle.crt

SetEnvIf User-Agent ".*MSIE.*" \
         nokeepalive ssl-unclean-shutdown \
         downgrade-1.0 force-response-1.0

CustomLog @LOGROOT@/ssl_request_log \
          "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"

</VirtualHost>                                  
